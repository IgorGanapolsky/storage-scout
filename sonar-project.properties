# SonarCloud Configuration
sonar.projectKey=IgorGanapolsky_storage-scout
sonar.organization=igorganapolsky

# Project info
sonar.projectName=Storage Scout
sonar.projectVersion=1.0.0

# Source paths (Python only - Flutter app was removed)
sonar.sources=tools_rental,digital_products
# No test directory currently

# Exclusions
# Note: product_generator.py excluded as it contains example code snippets with
# intentional credential parameters for demonstration purposes (not real code)
sonar.exclusions=\
  **/node_modules/**,\
  **/.venv/**,\
  **/__pycache__/**,\
  **/coverage/**,\
  **/data/**,\
  **/*.json,\
  **/product_generator.py

# Coverage (Python)
sonar.python.coverage.reportPaths=coverage.xml

# Encoding
sonar.sourceEncoding=UTF-8

# Quality Gate (non-blocking to avoid CI failures)
sonar.qualitygate.wait=false
